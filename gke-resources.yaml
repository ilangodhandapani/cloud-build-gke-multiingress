---
apiVersion: v1
kind: ConfigMap
metadata:
  name: myapp-cm
data:
  LCP: "QA"
  PORT: "8080"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
  labels:
    app: myapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: myapp
        image: us-central1-docker.pkg.dev/flawless-lacing-392113/my-app-repo/hello-multi-ingress:latest
        env:
        - name: LCP 
          valueFrom:
            configMapKeyRef:
              name: myapp-cm           
              key: LCP 
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: myapp-cm
              key: PORT
